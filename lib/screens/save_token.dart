import 'package:flutter/material.dart';

class SaveTokenScreen extends StatelessWidget {
  const SaveTokenScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("save token screen"),),
      body: Center(
        child: Text("save token screen"),
      ),
    );
  }
}
